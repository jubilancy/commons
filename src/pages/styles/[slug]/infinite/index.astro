---
import Base from "../../../../layouts/Base.astro";
import ViewSwitch from "../../../../components/ViewSwitch.astro";
import InfiniteView from "../../../../components/InfiniteView.astro";
import { getClassificationFacets, getByClassificationSlug } from "../../../../data/gallery";

export function getStaticPaths() {
  return getClassificationFacets().map((f) => ({ params: { slug: f.slug } }));
}

const { slug } = Astro.params;
const works = getByClassificationSlug(slug as string);
const label = works[0]?.classification ?? slug;
---
<Base title={`${label} — Infinite View`}>
  <p class="crumb"><a href="/styles/">← All styles</a></p>
  <h1>{label}</h1>
  <p class="count">{works.length.toLocaleString()} work{works.length === 1 ? "" : "s"}</p>
  <ViewSwitch base={`/styles/${slug}`} active="infinite" />
  <InfiniteView artworks={works} />
</Base>

<style>
  .crumb { font-size: 0.85rem; margin-bottom: 8px; }
  .crumb a { color: var(--muted); text-decoration: none; }
  .crumb a:hover { color: var(--accent); }
  .count { color: var(--muted); font-size: 0.9rem; margin-top: -4px; }
</style>
